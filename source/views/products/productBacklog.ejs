<!DOCTYPE html>
<html lang="en">
    <%- include("../includes/head.ejs")  %>
<body>
    <%- include("../includes/header.ejs") %> 
    <main>
        <section class="producto-total">
            
    
            <figure class="imagen">
                <img src="/assets/products-images/<%= product.image.path%>" alt="Grinder" >
            </figure>
            <article class="descripcion-producto">
                <h1><%= product.name %> </h1>
                <p class="precio">$ <%= product.price %> </p>
                <p class="descripcion-precio">(los impuestos no estan incluídos en el precio)</p>
                <% if (user && !user.admin) { %>
                    <form method="post" action="">
                        <label for="quantity">Cantidad: </label>
                        <input type="number" id="quantity" name="quantity" min="1" max="10">
                        <button class="boton" type="submit"><a class="hipervinculo" href="/chart"><i class="fa-solid fa-cart-shopping"></i> Agregar al Carrito</a></button>
                    </form>
                <% } %>
                
                <p class="informacion-producto"><%= product.description %> </p>

                <% if (user && user.admin) { %>
                    <nav class="botones">
                        <a class="boton-editar" href="/products/edit/<%=product.id%>">Editar Producto</a>
                        <form action="/products/delete/<%=product.id%>?m=DELETE" method="POST">
                            <button class="boton-eliminar" type="submit">Eliminar Producto</button>
                        </form>
                    </nav>
                 
                <% } %>
            </article>
            
        </section>
    </main>
    <%- include("../includes/footer.ejs") %> 
</body>
</html>